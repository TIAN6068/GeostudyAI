{
  "name": "geostudyai",
  "version": "1.0.0",
  "private": true,
  "description": "GeoStudy AI course mode — unified web server (static + DeepSeek proxy)",
  "main": "mentor-proxy.js",
  "scripts": {
    "start": "node mentor-proxy.js",
    "dev": "node mentor-proxy.js"
  },
  "engines": {
    "node": ">=18"
  }
}
