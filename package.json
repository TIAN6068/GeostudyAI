{
  "name": "geostudyai",
  "version": "1.0.0",
  "private": true,
  "description": "GeoStudy AI course mode — static front-end",
  "scripts": {
    "start": "serve -s -l $PORT ."
  },
  "dependencies": {
    "serve": "^14.2.4"
  },
  "engines": {
    "node": ">=18"
  }
}
